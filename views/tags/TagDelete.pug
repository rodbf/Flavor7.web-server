extend ../Layout

block content
    h1.title= title
    p Deseja deletar a tag #{tag.name}?

    form.sideBySideForm(method='POST' action='')
        button.btn.btn-primary(type='submit') Confirmar

    form.sideBySideForm(method='GET' action=tag.url)
        button.btn.btn-primary(type='submit') Cancelar
